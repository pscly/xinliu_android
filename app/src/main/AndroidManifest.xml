<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- minSdk=33：通知权限为运行时权限，必须在 manifest 声明后才能请求 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 日历联动：自动写入系统日历（需要运行时授权） -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <!-- 系统闹钟跳转：打开系统时钟 App 的“新建闹钟/计时器”界面（normal 权限） -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Todo 准点提醒（EXACT）：声明后用户可在系统设置中“允许精确闹钟” -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 设备重启后需要触发一次提醒重排（EXACT 闹钟会在重启后丢失） -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 应用内更新：下载并校验 APK 后交给系统安装器。 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".OneMemosApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.OneMemos">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- WorkManager：我们使用 Configuration.Provider（Hilt WorkerFactory），需要移除默认 Startup 初始化器 -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".share.ShareToOneMemosActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@style/Theme.OneMemos.QuickCapture">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.feature.quickcapture.QuickCaptureActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity="${applicationId}.quickcapture"
            android:theme="@style/Theme.OneMemos.QuickCapture"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".overlay.QuickCaptureOverlayEntryActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity="${applicationId}.quickcapture.overlay"
            android:theme="@style/Theme.OneMemos.Transparent" />

        <activity
            android:name=".overlay.QuickCaptureOverlayPickImagesActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity="${applicationId}.quickcapture.overlay"
            android:theme="@style/Theme.OneMemos.Transparent" />

        <activity
            android:name=".screenshot.ScreenshotQuickCaptureActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity="${applicationId}.quickcapture.screenshot"
            android:theme="@style/Theme.OneMemos.Transparent" />

        <service
            android:name=".overlay.QuickCaptureOverlayService"
            android:exported="false" />

        <service
            android:name=".qs.QuickCaptureTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_quick_capture"
            android:label="@string/qs_quick_capture"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".qs.QuickScreenshotTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_quick_screenshot"
            android:label="@string/qs_quick_screenshot"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>

</manifest>
