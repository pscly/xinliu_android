# 采用务实严格的模块边界

Android 多模块迁移完成后，工程采用“务实严格”的边界：UI 与 ViewModel 不直接调用 Retrofit API、持久化实现或 WorkManager 实现，而是通过面向业务能力的契约访问；无状态的 URL 处理、分页等技术类型可以作为少量、明确且不承载 I/O 或可变状态的例外。该选择在隔离副作用和避免形式化样板代码之间取得平衡，`:app` 继续作为唯一组合根。
