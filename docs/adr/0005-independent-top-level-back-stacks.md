# 顶层分区使用独立返回栈

随笔、锦囊、待办、个人中心、归档与设置分别保留自己的返回栈，用户通过抽屉切换分区时恢复该分区上次位置，详情页归属发起它的分区。位于非随笔分区根页面时，系统返回先切回并恢复随笔栈；只有在随笔根页面再次返回才退出应用，不维护额外的顶层分区访问历史。分享或 Todo 通知等外部入口保留全部分区现场，把目标压入对应分区栈；外部字符串必须先映射为受控 `NavKey`，未知值不得直接写入返回栈。
